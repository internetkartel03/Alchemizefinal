# lucide-react-native@0.475 declares peer support only for react <=18 while
# this project uses react 19; legacy resolution is required for install to
# succeed everywhere (EAS build, CI, local) without passing flags manually.
legacy-peer-deps=true

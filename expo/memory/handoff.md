# Handoff Memory

The repository is prepared for local engineering handoff.

Primary local commands:

```bash
cd expo
npm install --legacy-peer-deps
npm run validate:final
npm run lint
npx expo start
```

Do not ship before simulator/device QA is complete.
